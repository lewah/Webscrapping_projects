--------------------------conda env
# conda create --name webscrapping
# conda activate webscrapping
# conda deactivate

#packages
conda install libxml2 libxslt
conda install lxml
conda install requests
conda install beautifulsoup4

conda install -c conda-forge googlesearch
conda install -c conda-forge google-api-python-client

# cleaing conda
conda clean --all

-----------------------------pyhton env
python3 -m venv webscrapping
source webscrapping/bin/activate
deactivate


#packages
pip install beautifulsoup4
--pip install serpapi
pip install google-search-results  - Python 3.7+
pip install pandas








#For conda install requests  check the version of python running in the conda env and the version of python running on your IDE
python --version

#
python3 --version
python3 -m venv webscrapping

#
Solution: on your IDE add/select interpreter from working directory/virtual env where google-search-results was installed